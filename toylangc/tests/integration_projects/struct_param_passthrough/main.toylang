fn println_i32(x: i32)

struct Counter {
    value: i32,
}

fn make_counter() -> Counter {
    Counter { value: 42 }
}

fn identity(c: Counter) -> Counter {
    c
}

fn main() {
    let c = make_counter();
    let c2 = identity(c);
    println_i32(c2.value);
}
