[project]
name = "bench4b_blackbox_sky"
source = "main.toylang"
rust_caller = "rust_caller.rs"
features = ["allocator_api"]
opt-level = "3"
lto = "thin"
codegen-units = 16

[rust-dependencies]
test_helpers = { path = "../../test_helpers" }
