[project]
name = "bench4b_blackbox_rust_inlineable"
source = "main.toylang"
rust_caller = "rust_caller.rs"
opt-level = "3"
lto = "thin"
codegen-units = 16

[rust-dependencies]
test_widgets = { path = "../../test_widgets" }
