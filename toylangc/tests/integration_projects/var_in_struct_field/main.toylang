fn println_i32(x: i32)

struct Point {
    x: i32,
    y: i32,
}

fn make_point(x: i32, y: i32) -> Point {
    Point { x: x, y: y }
}

fn main() {
    let p = make_point(10, 20);
    println_i32(p.x);
    println_i32(p.y);
}
