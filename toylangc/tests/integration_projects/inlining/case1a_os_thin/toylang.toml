[project]
name = "case1a_os_thin"
source = "main.toylang"
rust_caller = "rust_caller.rs"
opt-level = "s"
lto = "thin"
codegen-units = 16

[rust-dependencies]
test_helpers = { path = "../../test_helpers" }
