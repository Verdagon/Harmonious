[project]
name = "case5_oz_nolto"
source = "main.toylang"
rust_caller = "rust_caller.rs"
opt-level = "z"
codegen-units = 16
features = ["allocator_api"]

[rust-dependencies]
test_helpers = { path = "../../test_helpers" }
