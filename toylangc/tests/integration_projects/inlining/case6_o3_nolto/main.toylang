use case6_lib_inl::Box
use case6_lib_inl::make_box
use case6_lib_inl::box_value
use some_rust_lib::duplicate

fn println_i32(x: i32)

fn main() {
    let b = make_box(42);
    let copy = duplicate<Box>(&b);
    println_i32(box_value(&copy));
}
