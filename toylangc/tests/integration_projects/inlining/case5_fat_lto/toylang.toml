[project]
name = "case5_fat_lto"
source = "main.toylang"
rust_caller = "rust_caller.rs"
opt-level = "3"
lto = "fat"
features = ["allocator_api"]

[rust-dependencies]
test_helpers = { path = "../../test_helpers" }
