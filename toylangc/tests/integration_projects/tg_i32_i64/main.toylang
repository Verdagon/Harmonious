fn println_i32(x: i32)
fn println_i64(x: i64)

struct Pair<A, B> {
    first: A,
    second: B,
}

fn make_pair() -> Pair<i32, i64> {
    Pair<i32, i64> { first: 10, second: 2000000000000 }
}

fn main() {
    let p = make_pair();
    println_i32(p.first);
    println_i64(p.second);
}
