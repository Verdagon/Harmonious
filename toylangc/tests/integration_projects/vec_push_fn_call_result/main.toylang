use std::alloc::Global
use std::vec::Vec

fn println_usize(x: usize)

struct Point {
    x: i32,
    y: i32,
}

fn make_point() -> Point {
    Point { x: 5, y: 6 }
}

fn main() {
    let v = Vec::new<Point, Global>();
    v.push(make_point());
    println_usize(v.len());
}
