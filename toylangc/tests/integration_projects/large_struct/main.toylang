fn println_i32(x: i32)

struct Big {
    a: i32,
    b: i32,
    c: i32,
    d: i32,
    e: i32,
    f: i32,
}

fn make_big() -> Big {
    Big { a: 1, b: 2, c: 3, d: 4, e: 5, f: 6 }
}

fn main() {
    let b = make_big();
    println_i32(b.a);
    println_i32(b.f);
}
