[project]
name = "b10_probe_fat"
source = "main.toylang"
features = ["allocator_api"]
opt-level = "3"
lto = "fat"
codegen-units = 16

[rust-dependencies]
test_helpers = { path = "../../test_helpers" }
