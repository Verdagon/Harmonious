fn println_i32(x: i32)

fn classify(x: i32) -> i32 {
    if x > 0 {
        1
    } else if x < 0 {
        2
    } else {
        0
    }
}

fn main() {
    println_i32(classify(5));
    println_i32(classify(-3));
    println_i32(classify(0));
}
