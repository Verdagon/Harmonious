use regex::Regex
use regex::Error
use std::result::Result
use std::io::stdout
use std::io::Stdout
use std::io::Write

fn main() {
    let re = Regex::new("\\d+").unwrap();
    Write::write_all(&stdout(), b"regex ok\n");
}
