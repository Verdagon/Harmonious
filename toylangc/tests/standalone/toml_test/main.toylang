use toml::from_str
use toml::Value
use toml::de::Error
use std::result::Result
use std::io::stdout
use std::io::Stdout
use std::io::Write

fn main() {
    let val = from_str<Value>("").unwrap();
    Write::write_all(&stdout(), b"toml ok\n");
}
